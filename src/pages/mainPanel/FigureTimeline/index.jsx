import React from 'react';
import FigureTimeline from './FigureTimeline';

const tmpProps = {
  extract_feature: {},
  chosenDescriptorId: {}
}

export default () => (
    <FigureTimeline {...tmpProps} />
  )