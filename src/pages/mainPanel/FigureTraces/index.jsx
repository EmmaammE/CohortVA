import React from 'react';
import FigureTraces from './FigureTraces';

const tmpProps = {
  extract_feature: {},
  chosenDescriptorId: {}
}

export default () => (
    <FigureTraces {...tmpProps} />
  )